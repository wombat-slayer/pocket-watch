SPRINT 9 INTEGRATION NOTES
==========================
Generated: 2026-05-25

TASK 1: Reports.jsx — Forecast & Net Worth Tabs Removed
--------------------------------------------------------
- Removed tab buttons: "Forecast" (tab='forecast') and "Net Worth" (tab='networth')
- Removed JSX render blocks for both tabs
- Removed canvas refs: canvasForecast, canvasNetWorth
- Removed useMemo: forecast (OLS regression), netWorthHistory (account-derived)
- Removed useChart calls for both canvasForecast and canvasNetWorth
- Removed props: accounts, goals (no longer needed by Reports)
- Props signature is now: { transactions, onCategoryDrillDown }
- App.jsx call updated: removed accounts={accounts} goals={goals} from <Reports>
- Remaining tabs: Trend, Category (By Category), YoY (Year vs Year)
- PDF export, tag filter, range selector all retained unchanged

TASK 2: Dashboard.jsx — Insights Panel Added
---------------------------------------------
New props accepted:
  - netWorthHistory: array of { date, netWorth, assets, debts } — default []
  - goals: array of { name, target, ... } — default []

New state:
  - insightsOpen: boolean, default true (controls panel collapse)

New canvas refs:
  - canvasForecast     — Spending Forecast line chart
  - canvasNWTrajectory — Net Worth Trajectory line chart

New useMemo computations:
  - forecastData: OLS linear regression on 6 months of expense data,
    projects 3 months forward. Returns { labels, actualData, forecastValues, slope, intercept }
  - nwTrajectoryData: sorts netWorthHistory by date, extracts labels/netWorth/assets/debts

New useChart calls:
  - canvasForecast: line chart, "Actual" (#c2735a solid) + "Forecast" (#7fa88b dashed)
  - canvasNWTrajectory: line chart, "Net Worth" (#7fa88b) + one dashed dataset per
    goal that has target > 0 (uses goal.name as label, flat line at goal.target)

New JSX at bottom of Dashboard (after Recent Transactions card):
  - Collapsible panel with header "📈 Insights" and chevron toggle
  - Two cards in a responsive auto-fit grid (min 320px per column):
    Card 1: Spending Forecast (canvas, 220px height)
    Card 2: Net Worth Trajectory (canvas, 220px height, or empty-state if no history)

TASK 3: App.jsx — Prop Wiring
------------------------------
Dashboard call updated to pass:
  - netWorthHistory={netWorthHistory}  ← already tracked in App state since Sprint 6
  - goals={goals}                      ← already tracked in App state

Reports call updated (accounts and goals removed):
  - Before: <Reports transactions={...} accounts={accounts} goals={goals} onCategoryDrillDown={...} />
  - After:  <Reports transactions={...} onCategoryDrillDown={...} />

No new state needed in App.jsx — both netWorthHistory and goals were already present.

SYNTAX VERIFICATION
-------------------
Babel JSX parse results (2026-05-25):
  src/components/Reports.jsx   : OK (223 lines)
  src/components/Dashboard.jsx : OK (439 lines)
  src/App.jsx                  : OK (635 lines)

NOTE: Both Dashboard.jsx and App.jsx were found pre-truncated (cut off mid-content)
before Sprint 9 edits began. Truncation was repaired as part of this sprint:
  - Dashboard.jsx: was cut off at line 315, mid template-literal in Savings Rate card
  - App.jsx: was missing closing </div> );  } at end of file
