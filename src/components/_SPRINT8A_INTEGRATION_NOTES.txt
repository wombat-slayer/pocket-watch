Sprint 8a Integration Notes
===========================

1. useCategoryMemory (src/hooks/useCategoryMemory.js)
   - Self-contained hook, imported directly in CSVImport.jsx
   - No App.jsx changes required

2. CSVImport.jsx
   - useCategoryMemory wired in — receives existingTxs prop (already passed from App.jsx)
   - Memory suggestions take priority over autoCategory()
   - Rows flagged with _memorized:true show a "💡 auto" amber badge in the review table
   - _memorized is stripped in doImport() alongside _csv and _isDuplicate

3. Recurring.jsx
   - Now accepts a `transactions` prop for recurring pattern detection
   - ACTION REQUIRED in App.jsx: add transactions={transactions} to the Recurring render call
     Example: <Recurring ... transactions={transactions} />
   - suggestionPrefill state pre-populates the Add Rule form when user clicks "+ Add Rule"
     on a detected pattern — no separate form component changes needed (uses existing
     RecurringForm `initial` prop)

4. No other App.jsx changes needed for Sprint 8a beyond the one listed above.
